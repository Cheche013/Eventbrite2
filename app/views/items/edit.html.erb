<% content_for :title, "Modifier l’item" %>

<div class="container py-4">
  <h1 class="h3 mb-4">Modifier l’item</h1>
  <%= render "form", item: @item %>
  <div class="mt-3 d-flex gap-3">
    <%= link_to "Voir", @item, class: "link-light" %>
    <%= link_to "Retour", items_path, class: "link-light" %>
  </div>
</div>
